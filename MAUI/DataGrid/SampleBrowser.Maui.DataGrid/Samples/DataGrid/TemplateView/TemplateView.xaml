<?xml version="1.0" encoding="utf-8" ?>
<localCore:SampleView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="SampleBrowser.Maui.DataGrid.SfDataGrid.TemplateView"
             xmlns:local="clr-namespace:SampleBrowser.Maui.DataGrid"
             xmlns:localCore="clr-namespace:SampleBrowser.Maui.Base;assembly=SampleBrowser.Maui.Base"
             xmlns:imageExtension="clr-namespace:SampleBrowser.Maui.Base.Converters;assembly=SampleBrowser.Maui.Base"
             xmlns:sfgrid="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid">

    <localCore:SampleView.BindingContext>
        <local:TemplateViewModel />
    </localCore:SampleView.BindingContext>

    <localCore:SampleView.Resources>
        <local:OrderInfoViewModel x:Key="viewmodel" />
        <imageExtension:SfImageSourceConverter x:Key="imageConverter"></imageExtension:SfImageSourceConverter>

        <Style TargetType="sfgrid:DataGridHeaderCell">
            <Setter Property="FontFamily"
                    Value="Roboto-Medium" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Padding">
                <Setter.Value>
                    <OnPlatform x:TypeArguments="Thickness">
                        <On Platform="iOS,WinUI,MacCatalyst"
                            Value="16,14" />
                        <On Platform="Android"
                            Value="16,17" />
                    </OnPlatform>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="sfgrid:DataGridCell">
            <Setter Property="FontSize"
                    Value="14" />
        </Style>

        <DataTemplate x:Key="DetailsViewTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="150" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="300" />
                </Grid.ColumnDefinitions>
                <Image Grid.Row="0"
                       Grid.RowSpan="2"
                       Grid.Column="0"
                       Margin="15"
                       HorizontalOptions="Start"
                       Source="{Binding  Data.DealerImage}">
                </Image>
                <Label Grid.Row="0"
                       Grid.Column="1"
                       HorizontalOptions="Start"
                       VerticalOptions="Center"
                       Text="Order Details"
                       FontAttributes="Bold">
                    <Label.Margin>
                        <OnPlatform x:TypeArguments="Thickness">
                            <!-- Define different margins for each platform -->
                            <On Platform="iOS,Android"
                                Value="20,0,0,0" />
                            <On Platform="WinUI,MacCatalyst"
                                Value="50,0,0,0" />
                        </OnPlatform>
                    </Label.Margin>
                </Label>
                <StackLayout Grid.Row="1"
                             Grid.Column="1"
                             HorizontalOptions="Start"
                             VerticalOptions="Center"
                             Orientation="Vertical">
                    <StackLayout.Margin>
                        <OnPlatform x:TypeArguments="Thickness">
                            <!-- Define different margins for each platform -->
                            <On Platform="iOS,Android"
                                Value="20,0,0,0" />
                            <On Platform="WinUI,MacCatalyst"
                                Value="50,0,0,0" />
                        </OnPlatform>
                    </StackLayout.Margin>
                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="Product Name :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.ElectonicProduct}"  />
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="Quantity :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.Quantity}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="Unit Price :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.ProductPrice}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="Discount :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.Discount, StringFormat='{}{0:0.00}'}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="Freight :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.Freight}"/>
                    </StackLayout>
                </StackLayout>
                <Label Grid.Row="0"
                       Grid.Column="2"
                       Margin="10,0,0,0"
                       HorizontalOptions="Start"
                       VerticalOptions="Center"
                       Text="Shipping Details"
                       FontAttributes="Bold" />
                <StackLayout Grid.Row="1"
                             Grid.Column="2"
                             Margin="10,0,0,0"
                             HorizontalOptions="Start"
                             VerticalOptions="Center"
                             Orientation="Vertical">
                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="End"
                               VerticalOptions="Center"
                               Text="Shipping City :"/>
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.ShipCity}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="End"
                               VerticalOptions="Center"
                               Text="Shipping Date :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.ShippedDate, StringFormat='{}{0:MM/dd/yyyy}'}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="End"
                               VerticalOptions="Center"
                               Text="Ship Country :"/>
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.ShipCountry}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="End"
                               VerticalOptions="Center"
                               Text="Ship Postal Code :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.ShipPortalCode}"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Label HorizontalOptions="End"
                               VerticalOptions="Center"
                               Text="Delivery Delay :" />
                        <Label Margin="5"
                               HorizontalOptions="Start"
                               VerticalOptions="Center"
                               Text="{Binding Data.DeliveryDelay}" />
                    </StackLayout>
                </StackLayout>
            </Grid>
        </DataTemplate>
    </localCore:SampleView.Resources>

    <localCore:SampleView.Content>
        <sfgrid:SfDataGrid x:Name="dataGrid"
                           AutoGenerateColumnsMode="None"
                           HeaderRowHeight="52"
                           DataGridLoaded="dataGrid_DataGridLoaded"
                           HorizontalScrollBarVisibility="Always"
                           VerticalScrollBarVisibility="Always"
                           ItemsSource="{Binding DealerInformation}"
                           ColumnWidthMode="Fill">
            <sfgrid:SfDataGrid.Columns>
                <sfgrid:DataGridNumericColumn HeaderText="Order ID"
                                              Format="D"
                                              MappingName="OrderID" />
                <sfgrid:DataGridTextColumn  HeaderText="Customer ID"
                                            MappingName="CustomerID" />
                <sfgrid:DataGridTextColumn  HeaderText="Name"
                                            MappingName="DealerName" />
                <sfgrid:DataGridTextColumn  HeaderText="Quantity"
                                            MappingName="Quantity">
                    <sfgrid:DataGridTextColumn.Visible>
                        <OnPlatform x:TypeArguments="x:Boolean">
                            <On Platform="iOS,Android"
                                Value="False" />
                            <On Platform="WinUI,MacCatalyst"
                                Value="True" />
                        </OnPlatform>
                    </sfgrid:DataGridTextColumn.Visible>
                </sfgrid:DataGridTextColumn>
                <sfgrid:DataGridTextColumn  HeaderText="Freight"
                                            MappingName="Freight">
                    <sfgrid:DataGridTextColumn.Visible>
                        <OnPlatform x:TypeArguments="x:Boolean">
                            <On Platform="iOS,Android"
                                Value="False" />
                            <On Platform="WinUI,MacCatalyst"
                                Value="True" />
                        </OnPlatform>
                    </sfgrid:DataGridTextColumn.Visible>
                </sfgrid:DataGridTextColumn>
            </sfgrid:SfDataGrid.Columns>
            
            <sfgrid:SfDataGrid.DetailsViewDefinition>
                <sfgrid:TemplateViewDefinition  RowTemplate="{StaticResource DetailsViewTemplate}" HeightMode="Auto" />
            </sfgrid:SfDataGrid.DetailsViewDefinition>
            
        </sfgrid:SfDataGrid>
    </localCore:SampleView.Content>

</localCore:SampleView>