<localCore:SampleView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SampleBrowser.Maui.Scheduler.SfScheduler.MonthInlineView"
             xmlns:local="clr-namespace:SampleBrowser.Maui.Scheduler.SfScheduler"
             xmlns:thickness ="clr-namespace:Microsoft.Maui;assembly=Microsoft.Maui"
             xmlns:localCore="clr-namespace:SampleBrowser.Maui.Base;assembly=SampleBrowser.Maui.Base"
             x:DataType="local:MonthInlineViewViewModel"
             xmlns:schedule="clr-namespace:Syncfusion.Maui.Scheduler;assembly=Syncfusion.Maui.Scheduler">

    <localCore:SampleView.Content>
        <Grid>
            <Grid.Margin>
                <OnPlatform x:TypeArguments="thickness:Thickness">
                    <On Platform="MacCatalyst" Value="-4,0,-4,0"/>
                    <On Platform="UWP" Value="-4,0,-6,0"/>
                </OnPlatform>
            </Grid.Margin>
            <Grid.BindingContext>
                <local:MonthInlineViewViewModel/>
            </Grid.BindingContext>
            <schedule:SfScheduler x:Name="Scheduler" x:DataType="local:MonthInlineViewViewModel"
                                  Grid.Row="0"
                                  ShowBusyIndicator="True"
                                  SelectedDate="{Binding SelectedDate}"
                                  AppointmentsSource="{Binding Events}"
                                  DisplayDate="{Binding DisplayDate}"
                                  AllowAppointmentDrag="False"
                                  View="Month">

                <schedule:SfScheduler.MonthView>
                    <schedule:SchedulerMonthView AppointmentDisplayMode="Indicator" ShowAppointmentsInline="True"/>
                </schedule:SfScheduler.MonthView>

                <schedule:SfScheduler.AppointmentMapping>
                    <schedule:SchedulerAppointmentMapping
                Subject="EventName"
                StartTime="From"
                EndTime="To"
                Background="Background"
                IsAllDay="IsAllDay"
                StartTimeZone="StartTimeZone"
                EndTimeZone="EndTimeZone"
                RecurrenceExceptionDates="RecurrenceExceptions"
                RecurrenceRule="RecurrenceRule"
                RecurrenceId="RecurrenceId"/>
                </schedule:SfScheduler.AppointmentMapping>
            </schedule:SfScheduler>
        </Grid>
    </localCore:SampleView.Content>
</localCore:SampleView>