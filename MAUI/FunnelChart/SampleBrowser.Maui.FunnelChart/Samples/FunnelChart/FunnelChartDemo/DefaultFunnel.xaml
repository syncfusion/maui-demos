<localCore:SampleView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SampleBrowser.Maui.FunnelChart.SfFunnelChart.DefaultFunnel"
             xmlns:local="clr-namespace:SampleBrowser.Maui.FunnelChart.SfFunnelChart"
             xmlns:localCore="clr-namespace:SampleBrowser.Maui.Base;assembly=SampleBrowser.Maui.Base" 
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             xmlns:chart="http://schemas.syncfusion.com/maui"
             x:DataType="local:ViewModel">

    <localCore:SampleView.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            <local:FunnelChartColorResources/>
        </ResourceDictionary.MergedDictionaries>
            <local:TooltipValueConverter x:Key="valueConverter"/>
            <DataTemplate x:Key="tooltipTemplate">
               <HorizontalStackLayout x:DataType="chart:TooltipInfo">
                    <Label Grid.Column="1"
                           Text="{Binding Item, Converter={StaticResource valueConverter}, ConverterParameter='Name'}"
                           FontSize="12"
                           TextColor="{AppThemeBinding Default={StaticResource ContentBackground}}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                    <Label Text="{Binding Item, Converter={StaticResource valueConverter}, ConverterParameter='Value', StringFormat=': {0}%'}"
                           FontSize="12"
                           TextColor="{AppThemeBinding Default={StaticResource ContentBackground}}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
               </HorizontalStackLayout>
            </DataTemplate>
        </ResourceDictionary>
    </localCore:SampleView.Resources>

    <localCore:SampleView.Content>
        <chart:SfFunnelChart x:Name="Chart" HorizontalOptions="Fill" VerticalOptions="Fill"
                         Margin="{OnPlatform Android='0,0,0,50', iOS='0,0,0,50', Default='0,0,0,0'}"
                         ShowDataLabels="True" 
                         PaletteBrushes="{AppThemeBinding Default={StaticResource PaletteBrushesLight1}, Dark={StaticResource PaletteBrushesDark1}}" 
                         StrokeWidth="2" Stroke="{AppThemeBinding Default={StaticResource ContentBackground}}" 
                         ItemsSource="{Binding FunnelData}" 
                         EnableTooltip="True"
                         TooltipTemplate="{StaticResource tooltipTemplate}"
                         XBindingPath="Name"
                         YBindingPath="Value">
            <chart:SfFunnelChart.BindingContext>
                    <local:ViewModel x:Name="ViewModel"/>
            </chart:SfFunnelChart.BindingContext>
            <chart:SfFunnelChart.Title>
                <Label Text="Website Conversion Rate" HorizontalOptions="Fill" HorizontalTextAlignment="Center" VerticalOptions="Center" FontSize="16" />
            </chart:SfFunnelChart.Title>
            <!--<chart:SfFunnelChart.Legend>
                <chart:ChartLegend Placement="{OnPlatform WinUI='Right',MacCatalyst='Right',Default='Bottom'}">
                </chart:ChartLegend>
            </chart:SfFunnelChart.Legend>-->
            <chart:SfFunnelChart.DataLabelSettings>
                <chart:FunnelDataLabelSettings UseSeriesPalette="False">
                    <chart:FunnelDataLabelSettings.LabelStyle>
                        <chart:ChartDataLabelStyle  LabelFormat="0'%" Margin="5" FontSize="16" TextColor="{AppThemeBinding Dark=white, Light=black}" Background="Transparent" />
                    </chart:FunnelDataLabelSettings.LabelStyle>
                </chart:FunnelDataLabelSettings>
            </chart:SfFunnelChart.DataLabelSettings>
            <chart:SfFunnelChart.TooltipBehavior>
                <chart:ChartTooltipBehavior Background="{AppThemeBinding Default={StaticResource ContentForeground}}"/>
            </chart:SfFunnelChart.TooltipBehavior>
        </chart:SfFunnelChart>
    </localCore:SampleView.Content>

    <localCore:SampleView.OptionView>
        <Grid>
            <VerticalStackLayout Spacing="10">
                <HorizontalStackLayout>
                    <Label Text="Landscape" FontSize="{OnPlatform Android='17', WinUI='17'}" Padding="{OnPlatform Android='5', WinUI='5'}" VerticalOptions="Center"/>
                    <buttons:SfCheckBox  x:Name="Inversed" IsChecked="False" StateChanged="Inversed_CheckedChanged" Margin="30,0,0,0" VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Grid>
    </localCore:SampleView.OptionView>
</localCore:SampleView>
